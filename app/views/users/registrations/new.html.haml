.error-title Désolé, nous ne vous avons pas retrouvé sur nos listes
.info Merci de bien vouloir nous préciser :
= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  = devise_error_messages!

  .field.clearfix
    = f.text_field :last_name, placeholder: 'Votre nom de famille'
    = f.text_field :first_name, placeholder: 'Votre prénom'

  .field.clearfix
    = f.text_field :telephone_number, placeholder: 'numero de téléphone'
    = f.email_field :email, :autofocus => true, placeholder: 'Email'

  .field.clearfix
    = f.text_field :association_name, placeholder: "Nom d'association"
    = f.text_field :licence, placeholder: 'Licence'


  .field.clearfix
    .radio-male
      = f.radio_button :male, "1"
      = f.label :male, 'Homme', class: "label"
    .radio-female
      = f.radio_button :male, "0"
      = f.label :female, 'Femme',  class: "label"
  .infolegal
    %span.asterisk *
    Merci d'indiquer un email valide, un email de confirmation vous sera envoyé à cette adresse pour validation définitive de votre inscription.
  .send
    = f.submit "Créer un compte", class: "send"
