.info Votre email a été confirmé, merci de bien vouloir nous préciser :
= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| 
  = devise_error_messages!

  .field.clearfix
    = f.text_field :last_name, placeholder: 'Votre nom de famille', required: true
    = f.text_field :first_name, placeholder: 'Votre prénom', required: true

  .field.clearfix
    = f.text_field :telephone_number, placeholder: 'numero de téléphone', required: true
    = f.email_field :email, :autofocus => true, placeholder: 'Email', required: true

  .field.clearfix
    = f.text_field :association_name, placeholder: "Nom d'association"
    = f.text_field :licence, placeholder: 'Licence'


  .field.clearfix
    .radio-male
      = f.radio_button :male, "1", required: true
      = f.label :male, 'Homme', class: "label"
    .radio-female
      = f.radio_button :male, "0", required: true
      = f.label :female, 'Femme',  class: "label"
  .infolegal
    %span.asterisk *
    Merci d'indiquer un email valide, un email de confirmation vous sera envoyé à cette adresse pour validation définitive de votre inscription.
  .send
    = f.submit "Créer un compte", class: "send"
